#include <vector>
#include <iostream>


using namespace std;


int main(void)
{
	
	
		for (int i = 0; i < a.size(); ++i) {
			vector<int>a(26, true);
			if (i % 3 == 0) a[i]=false;
		}
		

	for (int i = 0; i < a.size(); ++i) {
		vector<int>a(26, true);
		cout << a[i]<<endl;
	}
	cout << "\n";


	


}