#include <vector>
#include <set>
#include <unordered_set>

using namespace std;

int solution(int n, vector<int> lost, vector<int> reserve) {

	unordered_set<int> l(lost.begin(), lost.end());
	//순서가 유지되지 않은 상태로 lost의 vector로 초기화 시킴. 
	//집합화 한다.
	set<int> r;
	unordered_set<int> enter;



	return answer;

}