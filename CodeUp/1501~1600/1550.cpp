#include <stdio.h>

long long int n;

int sqrt(long long int n)
{
	float a;
	a=n;
	if(a==0) return 0;
	for(int i=0; i<=n; ++i){
		if((a-i)*(a-i)<=)
	}
	
}
// 이 부분에 들어가야 될 코드를 작성하여 제출
int main()
{
  scanf("%lld", &n);
  printf("%d\n", sqrt(n));
  return 0;
}
